<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link
      href='https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css'
      rel='stylesheet'
      integrity='sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx'
      crossorigin='anonymous'
    />
    <link href='../style.css' rel='stylesheet' />
    <title>Proyecto Backend</title>
</head>
<body>
{{> navbar}}
    <div class="container">
    <div class="row justify-content-md-center">
            <h1 class="pb-2 mb-4 text-danger border-bottom border-danger">Carrito</h1>
            <div class="table-responsive">
                <table class="table table-dark table-striped">
                    <tr>
                        <th>Nombre</th>
                        <th>Precio</th>
                        <th>Imagen</th>
                    {{#each productos}}
                    <tr>
                        <td>{{this.nombre}}</td>
                        <td>${{this.precio}}</td>
                        <td><img src={{this.foto}} alt="image" class="img"></td>
                    </tr>
                    <td>
                        <form action="/carrito/deleteproductos/{{_id}}" method="POST">
                            <button type="submit" class="btn btn-danger" > Eliminar </button>
                        </form>
                    </td>
                    {{/each}}
                </table>
            </div>
    </div>
        <h3>Total: {{total}}</h3>
    <form action="/carrito/buyCarrito" method="POST">
        <button type="submit" class="btn btn-dark">Comprar</button>
    </form>
</div>
    
    <script src="../../controllers/carritos.controller.js"></script>
</body>
</html>

